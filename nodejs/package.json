{
  "name": "aws-cloudfront-spaindex",
  "version": "1.0.0",
  "description": "A simple function that detects if a path was requested and returns it's index.html",
  "main": "index.js",
  "engines": {
    "node": "=8.10"
  },
  "scripts": {
    "test": "jest --collectCoverage=true --ci --runInBand --reporters=default --reporters=jest-junit",
    "cleanup": "rm -rf node_modules/ build/ coverage/ junit.xml *.zip env.js",
    "pack": "npm-pack-zip"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nuttybrewer/CloudFrontDirectoryIndexFunction.git"
  },
  "author": "Patrick Ethier",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/nuttybrewer/CloudFrontJWTValidator/issues"
  },
  "homepage": "https://github.com/nuttybrewer/CloudFrontJWTValidator#readme",
  "dependencies": {
    "fetch-mock": "^7.3.9",
    "jsonwebtoken": "^8.5.1",
    "node-fetch": "^2.6.1",
    "util": "^0.12.1"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "jest-junit": "^6.4.0",
    "npm-pack-zip": "^1.2.7"
  },
  "bundleDependencies": {
    "util": "^0.12.1"
  }
}
